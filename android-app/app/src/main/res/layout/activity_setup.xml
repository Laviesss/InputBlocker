<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/app_background">

    <view
        android:id="@+id/setup_view"
        class="com.inputblocker.app.SetupActivity$SetupView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <TextView
        android:id="@+id/tv_region_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="@color/app_surface"
        android:textColor="@color/app_text_primary"
        android:text="Regions: 0"
        android:textSize="14sp"/>

    <TextView
        android:id="@+id/tv_instructions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:background="@color/app_surface_elevated"
        android:textColor="@color/app_text_secondary"
        android:text="Draw rectangles on screen to define blocked regions"
        android:textSize="12sp"
        android:gravity="center"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/app_surface">

        <Button
            android:id="@+id/btn_undo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Undo"
            android:layout_margin="4dp"
            android:backgroundTint="@color/app_surface_elevated"
            android:textColor="@color/app_text_primary"/>

        <Button
            android:id="@+id/btn_clear"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Clear All"
            android:layout_margin="4dp"
            android:backgroundTint="#FF5722"/>

        <Button
            android:id="@+id/btn_save"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Save"
            android:layout_margin="4dp"
            android:backgroundTint="@color/accent_green"/>

        <Button
            android:id="@+id/btn_cancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Cancel"
            android:layout_margin="4dp"
            android:backgroundTint="@color/app_surface_elevated"
            android:textColor="@color/app_text_primary"/>
    </LinearLayout>

</LinearLayout>
