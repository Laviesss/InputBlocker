<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InputBlocker"
        tools:targetApi="34">

        <!-- Block Log Activity -->
        <activity
            android:name=".BlockLogActivity"
            android:exported="false"
            android:theme="@style/Theme.InputBlocker" />

        <!-- Community Gallery Activity -->
        <activity
            android:name=".CommunityGalleryActivity"
            android:exported="false"
            android:theme="@style/Theme.InputBlocker" />

        <!-- Detection Review Activity -->

        <activity
            android:name=".DetectionReviewActivity"
            android:exported="false"
            android:theme="@style/Theme.InputBlocker.FullScreen" />

        <!-- Sensing Activity for Ghost Tap Recording -->
        <activity
            android:name=".SensingActivity"
            android:exported="false"
            android:theme="@style/Theme.InputBlocker.FullScreen" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.InputBlocker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.inputblocker.ACTION_QUICK_MENU" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Setup Activity (Visual region drawing) -->
        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:theme="@style/Theme.InputBlocker.Overlay"
            android:screenOrientation="portrait" />

        <!-- Quick Settings Tile Service -->
        <service
            android:name=".QuickSettingsTileService"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Notification Receiver -->
        <receiver
            android:name=".NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.inputblocker.ACTION_TOGGLE_BLOCKING" />
                <action android:name="com.inputblocker.ACTION_SAFE_MODE" />
                <action android:name="com.inputblocker.ACTION_SYNC" />
            </intent-filter>
        </receiver>

        <!-- Overlay Service (Touch blocking) -->


        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="touchBlocking" />
            <meta-data
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_DESC"
                android:value="Touch blocking service" />
        </service>

        <service
            android:name=".VolumeButtonListenerService"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ShutdownReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Module Metadata (Vector/LSPosed + Legacy Xposed compatible) -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="InputBlocker — system-level ghost-tap filtering via Vector (LSPosed)" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
    </application>
</manifest>
